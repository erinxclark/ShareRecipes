<%= form_for @recipe, html: { multipart: true} do |f| %>
  <% if @recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe.errors.count, "error") %> prohibited this recipe from being saved:</h2>

      <ul>
      <% @recipe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <span class="input"><%= f.text_field :title %></span>
  </div>
  <div class="field">
    <%= f.label :author %><br>
    <span class="input"><%= f.text_field :author %></span>
  </div>
  <div class="field">
    <%= f.label :ingredients %><br>
    <span class="input"><%= f.text_area :ingredients %></span>
  </div>
  <div class="field">
    <%= f.label :instructions %><br>
    <span class="input"><%= f.text_area :instructions %></span>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <span class="input"><%= f.file_field :image %></span>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
